# Use this workspace to collaborate
workspace:
  id: ""

localResources:
  collections:
    - ../read-it-later-auth.postman_collection.json
  environments:
    - ../read-it-later.local.postman_environment.json

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ../postman/collections/Read It Later - Auth Flow: b35915f3-21fc-463c-b55b-a96115f98754
